apply plugin: 'java-library'
apply from: rootProject.file("maven.gradle")

dependencies {
    api 'com.android.tools.build:apksig:3.6.4'
}

targetCompatibility = rootProject.javaVersion
sourceCompatibility = rootProject.javaVersion